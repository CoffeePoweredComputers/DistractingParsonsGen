Collection: 
  [
    "for {iter_var} in {collection}"
  ]

Range: 
  #Done
  OneArg:
    [
      "for {iter_var} in range[{end}]",
      "for {iter_var} in range[{end}]:",
      "for {iter_var} in [{end}]:",
      "for {iter_var} in {end}:"
    ]
  #Done
  TwoArgs:
    [
      "for {iter_var} in range({end}, {start}):",
      "for {iter_var} in range[{start}, {end}]",
      "for {iter_var} in range[{start}, {end}]:",
      "for {iter_var} in [{start}, {end}]:"
    ]
  #Done but not used yet
  ThreeArgs:
    [
      "for {iter_var} in range({end}, {start}, {inc}):",
      "for {iter_var} in range({start}, {inc}, {end}):",
      "for {iter_var} in range[{start}, {end}, {inc}]:",
      "for {iter_var} in range[{start}, {end}, {inc}]",
      "for {iter_var} in [{start}, {end}, {inc}]:"
    ]

# Make 1 or two more
Enumerate: 
  [
    "for {index}, {value} in enumerate({args})",
    "for {value}, {index} in enumerate({args}):",
    "for {value}, {index} in enumerate[{args}]:",
    "for {index}, {value} in range({args}):",
    "for {index}, {value} in enumerate[{args}]:"
  ]

DictItems: 
  [
    "for {iter_var1}, {iter_var2} in items({dict})",
    "for {iter_var1}, {iter_var2} in items[{dict}]:",
    "for {iter_var1}, {iter_var2} in {dict}.items:"
  ]

DictKeys: 
  [
    "for {iter_var} in keys({dict})",
    "for {iter_var} in keys[{dict}]",
    "for {iter_var} in {dict}.keys"
  ]

DictValues: 
  [
    "for {iter_var} in items({dict})",
    "for {iter_var} in items[{dict}]",
    "for {iter_var} in {dict}.items"
  ]